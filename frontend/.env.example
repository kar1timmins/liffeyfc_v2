# Frontend Environment Variables Template
# DEVELOPMENT: Copy this file to .env.local and update values
# PRODUCTION: Set these values in your hosting platform (Railway, Vercel, etc.)

# ============================================
# PUBLIC VARIABLES (exposed to browser)
# Must be prefixed with PUBLIC_ for SvelteKit
# ============================================

# reCAPTCHA v3 site key (get from Google reCAPTCHA console)
PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# Debug logging (1 = enabled, 0 = disabled)
# Development: 1 (enable)
# Production: 0 (disable)
PUBLIC_DEBUG_LOGS=1

# Application Environment (development, staging, production)
PUBLIC_APP_ENV=development

# Backend API URL
# Development:
#   LOCAL: http://localhost:3000 (backend running separately)
#   DOCKER: http://localhost:3000 (via Vite proxy in dev mode)
# Production:
#   https://liffeyfcv2-production.up.railway.app (or your backend URL)
PUBLIC_API_URL=http://localhost:3000

# API endpoint for form submissions
PUBLIC_FORM_API_URL=/api/interest/submit/

# ============================================
# SERVER-SIDE VARIABLES (not exposed to browser)
# Do NOT prefix with PUBLIC_
# ============================================

# reCAPTCHA v3 secret key (server-side only)
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here

# Web3Forms access key (for contact form submissions)
WEB3FORMS_ACCESS_KEY=your_web3forms_key_here

# Resend API Key (for transactional emails like password reset)
RESEND_API_KEY=your_resend_api_key_here

